meta {
  name: PayMembershipSlot
  type: http
  seq: 3
}

patch {
  url: {{development}}/membership-plans/{{membershipId}}/record-payment
  body: json
  auth: bearer
}

headers {
  Authorization: Bearer {JWT_TOKEN}
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "amount": 500
  }
}
