meta {
  name: UpdateFieldRuleBookAllSlots
  type: http
  seq: 11
}

patch {
  url: {{development}}/fields/{{fieldId}}/rule-books/{{ruleBookAllSlotId}}
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
  ~Authorization: Bearer {{token}}
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "ruleName": "Weekend Premium Pricing",
    "slotSelectionType": "allSlots",
    "actionType": "fixedPrice",
    "value": 1500,
    "activeDays": ["saturday", "sunday"]
  }
}
