meta {
  name: ConfirmBooking
  type: http
  seq: 14
}

patch {
  url: {{development}}/bookings/{{slotId}}/confirm
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "extraAmount": 150
  }
}