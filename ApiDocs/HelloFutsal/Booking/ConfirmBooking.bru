meta {
  name: ConfirmBooking
  type: http
  seq: 14
}

patch {
  url: {{development}}/bookings/{{slotId}}/confirm
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "discount": true,
    "totalAmount": 150
  }
}
