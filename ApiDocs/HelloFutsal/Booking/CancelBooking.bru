meta {
  name: CancelBooking
  type: http
  seq: 5
}

patch {
  url: {{development}}/bookings/{{slotId}}/cancel
  body: none
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}
